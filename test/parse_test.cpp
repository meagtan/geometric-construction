#include "include/cli.h"
using namespace std;

void parse_test()
{
    CLIProgram p;
    string s;
    constr_num num;
    int pos = 0;

    /*
    {
        if (p.parse_num(&num, "1", pos, 1))
            cout << num << endl;
        else
            cout << "Invalid input." << endl;
    }
    */
}
